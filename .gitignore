# Secrets — never commit
.env

# Per-container state: databases, API keys, caches, logs
appdata/

# Local backups
*.bak
*.bak-*
*.orig

# Editor / OS noise
.DS_Store
*.swp

# Real monitor config (holds the ntfy topic)
storj-monitor.conf
